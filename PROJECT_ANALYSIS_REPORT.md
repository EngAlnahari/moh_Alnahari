# تقرير التحليل الهندسي للمشروع

> **نطاق التحليل:** قراءة وفهم فقط. لم يتم تعديل أو حذف أي ملف أثناء إعداد التحليل.

## ملخص تنفيذي

مساحة العمل `D:\EngMohammed` ليست تطبيقاً واحداً، بل بيئة تضم منصة ويب رئيسية، خدمة API مرافقة، أدوات Python لتصنيف الأراضي، نماذج واجهات أولية، وقواعد بيانات وملفات بيانات محلية. العلاقة التشغيلية الأساسية هي أن واجهة MVC في `Mntry_Awqaf` تستهلك خدمة REST في `TestProject/Test`.

## 1. الجرد والهيكل العام

تم جرد مساحة العمل كاملة، ونتيجته:

- **2,130 ملفاً** و**517 مجلداً**.
- **863 ملف مصدر أو بيانات أو توثيق**.
- ملفات ناتجة عن التطوير والبناء: `bin` (783 ملفاً)، و`obj` (390)، و`.vs` (44)، و`__pycache__` (6).

| المكوّن | الوصف |
|---|---|
| `Mntry_Awqaf/` | تطبيق الويب الرئيسي لمنصة تنجيز، مبني بـ ASP.NET Core MVC. |
| `TestProject/` | خدمة REST API ومشروع تجارب/اختبار مرتبط بمنصة تنجيز. |
| `LandClassificationProject/` | مشروع Python لتصنيف وتجزئة صور الأراضي. |
| `hhh/` | نماذج واجهات HTML مستقلة وتجريبية. |
| الجذر | توثيق، قواعد بيانات محلية ونسخ احتياطية، سكربتات SQL، تقارير، وملفات HTML اختبارية. |

## 2. المشاريع الموجودة

### 2.1 تطبيق MVC الرئيسي

- الحل: `Mntry_Awqaf/Mntry_Awqaf.sln`.
- المشروع: `Mntry_Awqaf/Mntry_Awqaf/Mntry_Awqaf.csproj`.
- يحتوي على 33 متحكماً، و56 نموذجاً، و113 واجهة Razor.
- مسؤول عن واجهة المستخدم والجلسات واستدعاء الـAPI، ويحتفظ بقاعدة SQLite مخصصة للنماذج الديناميكية.

### 2.2 خدمة API

- الحل: `TestProject/TestProject.sln`.
- المشروع: `TestProject/Test/Test.csproj`.
- يحتوي على 34 متحكماً و48 نموذجاً و8 ترحيلات قاعدة بيانات فعلية.
- يعمل كخدمة REST ويتصل بقاعدة SQL Server.

### 2.3 أدوات البيانات المساندة

- `TestProject/CheckData/CheckData.csproj`: أداة مستقلة لفحص البيانات.
- `TestProject/InsertArabicData/InsertArabicData.csproj`: أداة مستقلة لإدخال أو معالجة البيانات العربية.
- هذان المشروعان غير مضمّنين في ملف حل `TestProject.sln`.

### 2.4 مشروع تصنيف الأراضي

- المسار: `LandClassificationProject/`.
- يتضمن واجهة مكتبية Tkinter وواجهة FastAPI ومنطق تصنيف وتجزئة الصور.

### 2.5 النماذج الأولية

- المسار: `hhh/` وملفات `index_test*.html` و`temp.html` في الجذر.
- واجهات وتجارب مستقلة، ولا يظهر ارتباط تشغيلي مباشر لها بالتطبيقين الرئيسيين.

## 3. التقنيات ولغات البرمجة

- C# و.NET 8 وASP.NET Core.
- Python.
- HTML وRazor وJavaScript وCSS.
- SQL.
- SQL Server وSQLite.
- ملفات PDF وWord وصور مرفوعة وملفات ثابتة للواجهة.

## 4. أطر العمل والمكتبات

### تطبيق MVC

- ASP.NET Core MVC.
- Entity Framework Core 8.
- SQLite.
- Newtonsoft.Json.
- HttpClient وSessions.

### خدمة API

- ASP.NET Core Web API.
- Entity Framework Core 9.
- SQL Server.
- JWT Bearer و`jose-jwt`.
- Swagger/OpenAPI.

### الواجهة الأمامية

- Bootstrap.
- jQuery وjQuery Validation.
- CKEditor وTinyMCE.
- DataTables وإضافات واجهة عديدة.

### مشروع Python

- OpenCV وNumPy وMatplotlib وPillow.
- PyTorch وTorchvision.
- FastAPI وUvicorn.
- Segment Anything.
- `arabic-reshaper` و`python-bidi`.

## 5. قواعد البيانات والملفات المرتبطة بها

| الملف أو النظام | التقنية | الملاحظة |
|---|---|---|
| `Awqaf33.mdf` و`Awqaf33_log.ldf` | SQL Server | أصول تطوير محلية؛ حجم كل ملف 8 MB. |
| `SmartBusStation1.bak` | نسخة احتياطية SQL Server | حجمها نحو 3.3 MB، وعلاقتها الوظيفية غير موثقة. |
| `TestProject/Test` | SQL Server | الاتصال مضبوط على قاعدة `Awqaf33` محلياً. |
| `Mntry_Awqaf/.../dynamic_forms.db` | SQLite | قاعدة للنماذج الديناميكية، حجمها 20 KB. |
| `DynamicForms.db` و`Mntry_AwqafTest.db` | SQLite | ملفان فارغان، ويرجح أنهما بقايا أو ملفات تجريبية. |

توجد أيضاً صور وملفات PDF مرفوعة في `TestProject/Test/Uploads` و`TestProject/Test/Pdfs`، إضافة إلى مرفقات داخل `Mntry_Awqaf/.../wwwroot/uploads`.

## 6. الملفات الرئيسية ووظائفها

- `Mntry_Awqaf/Mntry_Awqaf/Program.cs`: يهيئ تطبيق MVC وSQLite والجلسات وHttpClient والمسارات.
- `Mntry_Awqaf/.../Controllers/BaseApiController.cs`: طبقة مساعدة لعمليات HTTP نحو خدمة الـAPI.
- `Mntry_Awqaf/.../Controllers/CommonController.cs`: عمليات CRUD مشتركة لعدد من شاشات الواجهة.
- `Mntry_Awqaf/.../Data/DynamicFormsDbContext.cs`: سياق SQLite لحقول النماذج الديناميكية وتخصيصاتها.
- `TestProject/Test/Program.cs`: يهيئ API وSQL Server وJWT وSwagger ومجلدي الرفع، ويحتوي على إنشاء وإدخال بيانات تجريبية.
- `TestProject/Test/Data/DbContextFirst.cs`: السياق الأساسي لـEntity Framework ويحتوي معظم كيانات النظام.
- `TestProject/Test/Migrations/`: تاريخ تطوير مخطط قاعدة البيانات.
- `LandClassificationProject/api.py`: API محلي يستقبل صورة ويعيد صورة بحدود المناطق المكتشفة.
- `LandClassificationProject/main.py`: واجهة مكتبية لاختيار الصور وتحليلها.
- `land_classifier.py` و`land_classifier_online.py` و`rule_engine.py`: منطق التصنيف والتجزئة والقواعد.
- `run_all.bat`: ملف تشغيل موجود في الجذر ويحتاج مراجعة قبل اعتماده كآلية تشغيل رسمية.

## 7. العلاقة بين المشاريع

```text
Mntry_Awqaf (واجهة MVC)
  ├─ SQLite: dynamic_forms.db
  └─ HTTP REST ──> TestProject/Test (خدمة API)
                    └─ SQL Server: Awqaf33

LandClassificationProject: مستقل حالياً
hhh وملفات HTML الاختبارية: نماذج أولية مستقلة
```

تستدعي واجهة MVC خدمة الـAPI عبر `HttpClient` في عمليات المستخدمين والعروض والعقود وأوامر تنجيز والحدود والإشعارات والنماذج. يوجد تكرار كبير في نماذج المجال بين التطبيقين: **41 اسماً مشتركاً**، ما يدل على أنهما طبقتان لنفس المجال دون مشروع عقود أو نماذج مشترك.

## 8. الحالة الحالية

- الفرع الحالي هو `main` وحالة Git كانت نظيفة وقت التحليل.
- توجد وثائق تأسيسية: `ARCHITECTURE.md` و`PROJECT_CONTEXT.md` و`ASSETS.md`.
- أحدث تعديلات المصدر الظاهرة كانت في 23 يونيو 2026، وتركزت على العقود ونماذج العمل والعروض.
- توجد مخرجات Debug وRelease، مما يدل على تشغيل أو بناء سابق.
- لم يتم تنفيذ Build أو تشغيل للتطبيقات ضمن هذا التحليل، لأن المطلوب كان القراءة فقط ولأن البناء قد ينشئ أو يحدّث ملفات.
- المستودع يمثل مساحة عمل متعددة المكوّنات، وليس منتجاً واحداً جاهزاً للتشغيل بخطوة واحدة.

## 9. المشاكل والمخاطر

### مخاطر عالية الأولوية

1. أسرار JWT وكلمات مرور أو بيانات اتصال موجودة في الإعدادات أو في منطق التطبيق؛ يجب تدويرها ونقلها إلى User Secrets أو متغيرات البيئة.
2. كلمات المرور مخزنة وتتم مقارنتها كنص واضح، كما تظهر حسابات تجريبية بكلمات مرور ثابتة.
3. لم يظهر استخدام فعلي لـ`[Authorize]` في متحكمات الـAPI، رغم إعداد JWT؛ وقد تكون نقاط API الحساسة غير محمية.
4. يوجد تسجيل Debug لكلمة مرور في `UserController` داخل واجهة MVC.
5. عنوان الـAPI غير موحد: الإعداد يشير إلى `http://localhost:5046/api/`، في حين يثبت `CommonController` عنوان `https://localhost:7027/api/`.
6. يوجد تعارض محتمل بين إعادة توجيه MVC إلى HTTPS واستخدام عنوان API افتراضي عبر HTTP.

### مخاطر معمارية وتشغيلية

1. استخدام `EnsureCreated()` في API مع وجود EF Migrations؛ هذا يربك إدارة مخطط قاعدة البيانات والنشر بين البيئات.
2. إدخال بيانات تجريبية وحسابات ثابتة تلقائياً عند التشغيل إذا كانت قاعدة البيانات فارغة.
3. وجود ملفات رفع وقواعد بيانات ومخرجات بناء داخل شجرة المشروع يخلط المصدر مع البيانات التشغيلية.
4. تكرار 41 نموذجاً بين MVC وAPI يرفع خطر انحراف العقود وتضارب التغييرات.
5. توجد مؤشرات لمشكلات ترميز عربية في بعض النصوص والتعليقات، إضافة إلى سكربتات مخصصة لإصلاح الترميز.
6. مشروع تصنيف الأراضي يعتمد على ملف أوزان خارجي غير محفوظ في Git (`sam_vit_b_01ec64.pth`)؛ ولذلك لا يعمل من المستودع وحده.
7. تسميات غير متسقة مثل `WorkWagesTaple` و`ownership` و`DbContextFirst` تقلل الوضوح وقابلية الصيانة.

## 10. خطة أولية لإعادة التنظيم والتطوير

1. اعتماد خريطة معمارية رسمية تحدد أن `TestProject/Test` هو الـAPI و`Mntry_Awqaf` هو الواجهة، أو اتخاذ قرار موثق بدمجهما.
2. معالجة الأمان أولاً: تدوير الأسرار، نقلها خارج المستودع، تشفير كلمات المرور، وتطبيق المصادقة والتفويض على endpoints.
3. توحيد إعدادات الاتصال وعناوين API حسب البيئة، وإزالة العناوين الثابتة من Controllers.
4. إنشاء مشروع مشترك لعقود الـAPI وDTOs لتقليل تكرار نماذج المجال.
5. اعتماد EF Migrations فقط لقاعدة SQL Server وإيقاف `EnsureCreated` بعد التحقق من مسار الترحيل.
6. فصل البيانات التشغيلية وملفات الرفع والمخرجات عن المصدر، ووضع سياسة نسخ احتياطي وتخزين واضحة.
7. تنظيم النماذج الأولية في مسار `prototypes/` أو أرشفتها بعد تحديد ما سيتم اعتماده.
8. توثيق تشغيل موحد يحدد المتطلبات وترتيب التشغيل والمنافذ وبيانات التطوير واستعادة قاعدة البيانات.
9. إضافة اختبارات تكامل للـAPI واختبارات للمسارات الحرجة في الواجهة ثم بناء CI.
10. إبقاء مشروع تصنيف الأراضي مستقلاً في المرحلة الحالية، ثم تحديد عقد API وبيئة تشغيل وأسلوب إدارة أوزان النموذج قبل أي دمج.
