# خطة تنظيف وتنظيم المشروع قبل التطوير

**النطاق:** `D:\EngMohammed`، `D:\EngMohammed_Assets_Extracted`، و`D:\EngMohammed_Assets\alnahari22-05-2026`  
**الحالة:** خطة تحليلية فقط. لم يتم نقل أو حذف أو تعديل أي ملف.  
**تاريخ الإعداد:** 24 يوليو 2026

## 1. مبدأ القرار

المرجع التطويري الحالي هو مستودع `D:\EngMohammed` وفرع Git `main`. أما `EngMohammed_Assets_Extracted` و`D:\EngMohammed_Assets\alnahari22-05-2026` فهما لقطات تاريخية/مصادر استعادة، وليستا فرعين بديلين للتطوير. أي تنظيف مستقبلي يجب أن يسبقه:

1. نسخة احتياطية غير قابلة للتعديل.
2. فحص Git وSHA-256 للملفات المراد نقلها.
3. تحقق من التشغيل والاستعادة في بيئة منفصلة.
4. موافقة صريحة على حذف أي ملف.

## 2. ما يجب أن يبقى داخل GitHub

يجب أن يبقى في مستودع الكود كل ما يلزم لإعادة بناء النظام، مع إزالة الأسرار والبيانات الحقيقية:

| النطاق | ما يبقى | ملاحظات |
|---|---|---|
| تطبيق MVC | `Mntry_Awqaf/Mntry_Awqaf.sln`، `Mntry_Awqaf/Mntry_Awqaf/*.csproj`، `Controllers/`، `Models/`، `Data/`، `Views/`، `Properties/`، `wwwroot/` الضروري، وملفات Razor/C# | هو واجهة Tanjez الرئيسية؛ تبقى ملفات المصدر وموارد الواجهة المرخصة |
| خدمة API | `TestProject/TestProject.sln`، `TestProject/Test/Test.csproj`، `Controllers/`، `Models/`، `Data/`، `Migrations/`، `Service/`، `Program.cs` | تبقى migrations وDTOs/العقود بعد تنظيمها |
| أدوات البيانات | `TestProject/CheckData` و`TestProject/InsertArabicData` وSQL المعاد مراجعته | يجب ألا تحتوي السكربتات على أسرار أو بيانات شخصية حقيقية |
| مشروع الذكاء الاصطناعي | `LandClassificationProject/*.py` و`requirements.txt` وملف إعداد نموذجي | يبقى كود الاستدلال؛ أوزان النموذج خارجة عن Git |
| النماذج الأولية | `hhh/` فقط إذا كانت ما زالت مرجع تصميم، وإلا تنقل للأرشيف بعد قرار المنتج | لا تخلط prototypes مع تطبيق الإنتاج |
| التوثيق | `ARCHITECTURE.md`، `PROJECT_CONTEXT.md`، `ASSETS.md`، تقارير التحليل، وملفات README/سياسات المساهمة | تحديثها بعد أي قرار معماري |
| البناء والإدارة | `.gitignore`، ملفات الحلول والمشروعات، manifests، migrations، قوالب `appsettings.Example.json` وCI | يمنع تضمين `appsettings.json` السري |
| جذر المشروع | سكربتات SQL القابلة لإعادة الإنتاج والوثائق فقط | مراجعة `run_all.bat` قبل اعتباره مسار تشغيل رسمي |

لا تبقى داخل GitHub: `bin`، `obj`، `.vs`، `__pycache__`، قواعد البيانات التشغيلية، النسخ الاحتياطية، أوزان النماذج، ملفات الرفع الحقيقية، الأسرار، وملفات النشر الشخصية.

## 3. ما يجب نقله إلى مستودع Assets منفصل

| الأصل | المالك/المستهلك | أسلوب الإدارة المقترح |
|---|---|---|
| `sam_vit_b_01ec64.pth` (357.67 MB) | `LandClassificationProject` | مستودع نماذج أو Object Storage خاص، مع SHA-256 ونسخة النموذج وmanifest؛ Git LFS خيار ثانوي |
| `alnahari22-05-2026.rar` (396.07 MB) | استعادة `Mntry_Awqaf` و`TestProject` | مخزن أرشيفي للقراءة فقط، مع تاريخ ومصدر وchecksum |
| `D:\EngMohammed_Assets\alnahari22-05-2026\` | لقطة تاريخية | حفظ نسخة واحدة؛ لا تستخدم للتطوير ولا تكررها في Git |
| قواعد البيانات والنسخ | `Awqaf33` وبيانات التطوير | Backup Vault مشفر بصلاحيات محدودة؛ Git يحفظ migrations وseed المنقح فقط |
| `Uploads/` و`Pdfs/` وصور `wwwroot/uploads` الحقيقية | API وMVC | Media/Object Storage مع metadata؛ يمنع نشر بيانات المستخدمين داخل المصدر |

مستودع Assets يجب أن يحتوي manifest مثل: الاسم المنطقي، الإصدار، الحجم، SHA-256، المستهلك، الترخيص، ومصدر الاسترجاع.

## 4. ما يجب أرشفته فقط

الأرشفة لا تعني الحذف ولا النقل الفوري؛ تعني الاحتفاظ بنسخة مرجعية غير قابلة للتطوير:

- `alnahari22-05-2026.rar` والنسخة المفكوكة التاريخية.
- `EngMohammed_Assets_Extracted` بعد إثبات أنه نسخة فرعية؛ يحتفظ به مؤقتًا حتى اعتماد الأرشيف الرئيسي.
- تقارير Word/PDF القديمة وتقارير التحليل السابقة.
- ملفات النماذج القديمة مثل `Create_Old.cshtml` و`Create_Backup.cshtml` و`Create_Enhanced_Backup.cshtml` و`Create_Complete.cshtml` بعد مقارنة السلوك.
- ملفات HTML الاختبارية `index_test.html` إلى `index_test5.html` و`temp.html` ونماذج `hhh` التي لا يعتمد عليها مسار إنتاج.
- `db_output.json` و`fields_output.json` وملفات نتائج الفحص، إن ثبت أنها snapshots وليست مدخلات تشغيل.
- النسخ المنشورة/المجمعة داخل اللقطة التاريخية؛ تحفظ فقط عند الحاجة إلى تدقيق إصدار بعينه.

يجب إضافة سجل أرشيف يوضح سبب الاحتفاظ، التاريخ، والنسخة المعتمدة، بدل ترك الملفات القديمة مختلطة مع المصدر.

## 5. الملفات المكررة بين المجلدات الثلاثة

### 5.1 النتيجة الرقمية

- المجلد الرئيسي يحتوي كود الإصدار الحالي، بينما `EngMohammed_Assets_Extracted` يحتوي نسخة جزئية تاريخية.
- بالمقارنة بالبصمة والمسار للمحتوى غير البنائي، يتطابق مجلد الاستخراج مع **163 ملفًا** في اللقطة الموسعة داخل `D:\EngMohammed_Assets\alnahari22-05-2026`.
- اللقطة الموسعة تحتوي **602 ملفًا إضافيًا** غير موجود في الاستخراج بعد استبعاد `bin/obj/.vs/.git/__pycache__`، لذلك الاستخراج ليس نسخة كاملة.
- بين المستودع الحالي والاستخراج، **158 ملفًا** متطابق المحتوى، و**3 ملفات** لها نفس المسار لكن محتوى تاريخي مختلف، وملفان (`.gitattributes` و`.gitignore` التاريخيان) لا يقابلهما مسار مطابق في الرئيسي.

### 5.2 أمثلة التكرار أو الاختلاف

| الفئة | أمثلة | القرار المقترح |
|---|---|---|
| كود C# وRazor | Controllers، Models، Views في `Mntry_Awqaf` و`TestProject` | اعتماد نسخة `D:\EngMohammed` كمرجع؛ اللقطات للقراءة فقط |
| إعدادات/سياق تاريخي | `appsettings.json`، `TestProject/Test/Data/DbContextFirst.cs`، `DbContextFirstModelSnapshot.cs` | مقارنة واعتماد الحالي؛ لا استبدال آلي |
| موارد الواجهة | `wwwroot/assets` وملفات Bootstrap/jQuery | الاحتفاظ بمصدر مرخص واحد، وإزالة النسخ المجمعة من الأرشيف المستقبلي |
| مخرجات البناء | DLL/PDB/JSON داخل `bin` و`obj` و`PubTmp/Out` | لا تعتبر مصدرًا؛ تعاد توليدها |
| Git metadata | `.git` و`.vs` و`.gitattributes/.gitignore` التاريخية | لا تدمج Git history من اللقطة دون قرار مستقل |

## 6. تحليل مجلدات `bin` و`obj` و`.vs` و`__pycache__`

| المجلد | الموجود حاليًا في الرئيسي | الوظيفة | خطة الحالة |
|---|---:|---|---|
| `bin` | 783 ملفًا تقريبًا ضمن التقرير التحليلي (نحو 203 MB في المشاريع الحالية) | DLL/EXE وملفات تشغيل ناتجة عن build/publish | يبقى محليًا فقط، ويعاد إنشاؤه في CI؛ لا Git ولا Assets |
| `obj` | 390 ملفًا تقريبًا (نحو 72 MB) | cache وintermediate outputs و`project.assets.json` | يبقى محليًا فقط؛ لا يؤرشف |
| `.vs` | 44 ملفًا تقريبًا (نحو 9.5 MB) | فهارس Visual Studio وSQLite وحالة المستخدم | محلي/قابل لإعادة التوليد؛ لا Git ولا Assets |
| `__pycache__` | 6 ملفات (نحو 20 KB) | bytecode Python | محلي/قابل لإعادة التوليد؛ لا Git ولا Assets |

وجود هذه المجلدات داخل اللقطات التاريخية يضخم الحجم، خصوصًا `obj` في اللقطة الموسعة. يجب أن تتضمن أي عملية تجميع أرشيفية قائمة استثناء لهذه المجلدات، وأن يكون CI قادرًا على البناء من مصدر نظيف.

## 7. مراجعة قواعد البيانات

| النوع | الملفات | الاستخدام | المعالجة المستقبلية |
|---|---|---|---|
| SQL Server data | `Awqaf33.mdf` و`Awqaf33_log.ldf`، كل منهما 8 MB | قاعدة `Awqaf33` التي يستهدفها `TestProject` | نقل لنسخ احتياطي مشفر؛ لا توضع في Git؛ حفظ migrations/seed المنقح |
| SQL Server backup | `SmartBusStation1.bak` (نحو 3.15 MB) | لا يوجد مرجع برمجي مباشر | أرشفة مؤقتة مع توثيق المالك؛ حذف مستقبلي فقط بعد تأكيد عدم الحاجة |
| SQLite | `dynamic_forms.db` (20 KB) | `Mntry_Awqaf`/النماذج الديناميكية | تحديد الملف canonical؛ لا يحتفظ بقاعدة تشغيلية داخل Git |
| SQLite فارغة | `DynamicForms.db` و`Mntry_AwqafTest.db` (0 بايت) | بقايا/ملفات اختبار محتملة | مرشحة للحذف بعد إثبات عدم استخدامها وإغلاق التطبيقات |

ينبغي توحيد مصدر مخطط SQL Server على EF Migrations (بدل خلطه مع `EnsureCreated`) وإنشاء seed آمن لا يتضمن بيانات شخصية أو كلمات مرور حقيقية.

## 8. مراجعة الملفات الحساسة

### `appsettings.json` وملفات الإعداد

- `TestProject/Test/appsettings.json` يحتوي اتصال SQL Server محليًا، اسم مستخدم، كلمة مرور، ومفتاح JWT ثابت.
- `Mntry_Awqaf/Mntry_Awqaf/appsettings.json` يحتوي عنوان API؛ يجب أن يكون لكل بيئة إعداد منفصل دون أسرار.
- ملفات `appsettings.Development.json` وملفات النشر (`PublishProfiles`) تحتاج مراجعة للتأكد من عدم احتوائها على رموز أو عناوين نشر حساسة.

### الأسرار وكلمات المرور

- تقرير التحليل أشار إلى كلمات مرور ثابتة، مقارنة نصية مباشرة، وتسجيل Debug لكلمة مرور في `UserController`.
- تظهر كلمات مرور ومفاتيح JWT وConnection Strings في إعدادات/منطق التطبيق؛ لا ينبغي نسخ قيمها إلى تقرير أو commit جديد.
- يجب تدوير كل الأسرار المكشوفة، ثم نقلها إلى User Secrets محليًا وSecret Manager/متغيرات بيئة في النشر، مع إضافة `appsettings.Example.json` بقيم placeholder فقط.

### Connection Strings

- تُستخدم قاعدة `Awqaf33` محليًا، ويوجد أيضًا اتصال مستضاف معلق في الإعدادات؛ يجب إزالة الحسابات وكلمات المرور من الملفات.
- يجب تمرير الاتصال عبر configuration providers، وتوحيد عنوان API لكل بيئة بدل تثبيته داخل Controllers.
- يجب فحص تاريخ Git، لأن حذف السر من النسخة الحالية لا يزيله من commit قديم؛ عند وجود تعرض حقيقي يلزم تدوير السر وإبطال القديم.

## 9. ما يمكن حذفه مستقبلًا بعد التأكد

هذه قائمة مرشحين فقط، وليست إذنًا بالحذف:

1. مجلدات `bin` و`obj` و`.vs` و`__pycache__` في جميع النسخ، بعد إغلاق IDE والتأكد من نجاح build نظيف.
2. `DynamicForms.db` و`Mntry_AwqafTest.db` الفارغان بعد التحقق من عدم وجود عملية تعتمد عليهما.
3. النسخ التجريبية المتعددة (`index_test*.html`، `temp.html`، ومسودات Razor) بعد اعتماد بديل وتسجيل قرار الأرشفة.
4. `SmartBusStation1.bak` فقط بعد تحديد مالكه والتأكد من وجود نسخة احتياطية موثقة بديلة.
5. ملفات البناء والنشر داخل اللقطات التاريخية بعد حفظ الأرشيف الأصلي والتحقق من أن المطلوب هو المصدر فقط.
6. `db_output.json` و`fields_output.json` وملفات التشخيص إذا ثبت أنها قابلة لإعادة التوليد وليست مدخلات تشغيل.
7. الملفات الفارغة أو ذات الأسماء المؤقتة مثل `git` و`notepad` بعد مراجعة تاريخ Git واستخداماتها، وليس بمجرد صغر الحجم.

لا يُحذف أي كود أو قاعدة بيانات أو أصل تاريخي اعتمادًا على الاسم فقط.

## 10. الهيكل المستقبلي المتوافق مع منظومة محور Mahwar

التنظيم المقترح يحافظ على استقلال الأنظمة مع بنية تشغيل مشتركة:

```text
mahwar-platform/                 # مستودع Git للكود والوثائق
├─ apps/
│  ├─ tanjez-mvc/                # Mntry_Awqaf
│  ├─ tanjez-api/                # TestProject/Test
│  ├─ data-tools/                # أدوات CheckData وInsertArabicData
│  └─ land-classification/       # تطبيق Python مستقل
├─ prototypes/                   # hhh وما يتم اعتماده من النماذج
├─ contracts/                    # DTO/OpenAPI وعقود التكامل المشتركة
├─ database/
│  ├─ migrations/
│  └─ seed-safe/
├─ docs/
├─ infra/                        # CI/CD وقوالب الإعداد، بلا أسرار
└─ assets.manifest.json

mahwar-assets/                   # مستودع/تخزين منفصل مقيد
├─ models/
├─ archives/
├─ database-backups/
└─ media/
```

قواعد الإدارة:

- لكل نظام Mahwar كوده وقاعدة بياناته ودورة نشره، مع عقود تكامل موثقة فقط بين الأنظمة.
- يبقى `tanjez-mvc` و`tanjez-api` مستقلين حتى اعتماد قرار دمج موثق؛ مشروع `contracts` يقلل تكرار النماذج الـ41 المشتركة.
- `LandClassificationProject` يبقى خدمة مستقلة، ويستهلك النموذج من `mahwar-assets` عبر manifest والتحقق من checksum.
- لا تُستخدم اللقطات التاريخية كمجلدات عمل؛ تُستعاد إلى workspace مؤقت للتحقق فقط.
- CI يبني من clone نظيف، ويفشل إذا وجد أسرارًا أو ملفات build متتبعة، ويُنشئ artifacts خارج Git.

## 11. ترتيب التنفيذ المقترح (مستقبلي فقط)

1. تجميد لقطة مرجعية وتوثيق hashes وحالة Git.
2. تدوير الأسرار ونقل Connection Strings وJWT إلى إدارة أسرار.
3. فصل/تحديد قواعد البيانات واعتماد migrations وseed آمن.
4. اعتماد النسخة الحالية من الكود، ثم أرشفة اللقطات التاريخية ومخرجات البناء.
5. توحيد manifests وعقود API ومسارات الإعداد حسب البيئة.
6. تنظيف المرشحين القابلين للحذف على دفعات صغيرة مع تحقق بعد كل دفعة.
7. تفعيل CI من مصدر نظيف واختبارات تكامل قبل بدء التطوير الميزاتي.

**النتيجة المطلوبة:** مستودع Git صغير وقابل للبناء، مستودع Assets موثق ومقيد، أرشيف تاريخي واحد، وقواعد بيانات/وسائط منفصلة؛ دون فقدان أي أصل قبل إثبات عدم الحاجة إليه.
